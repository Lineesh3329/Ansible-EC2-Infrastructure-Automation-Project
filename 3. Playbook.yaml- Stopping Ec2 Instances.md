### This `shutdown.yaml` playbook file is to stop running two Ubuntu Instances
      ---
      - hosts: all
        become: true
      
        tasks:
          - name: Shutting down only Ubuntu Instances
            ansible.builtin.command: /sbin/shutdown -t now
            when:
             ansible_facts['os_family'] == "Debian"
